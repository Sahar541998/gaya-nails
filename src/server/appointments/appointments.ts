import "server-only";

export { createAppointment } from "@/server/appointments/create-appointment";
export { cancelAppointment } from "@/server/appointments/cancel-appointment";
export { rescheduleAppointment } from "@/server/appointments/reschedule-appointment";
export { getAppointments } from "@/server/appointments/get-appointments";
